import { BlockTag, EIP1193ProviderRequestFunc } from './types';
declare const detectProxyTarget: (proxyAddress: string, jsonRpcRequest: EIP1193ProviderRequestFunc, blockTag?: BlockTag) => Promise<string | null>;
export default detectProxyTarget;
