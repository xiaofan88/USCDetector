import { BlockTag, DetectProxyTarget, EIP1193ProviderRequestFunc } from "./types";
declare function detectProxyTarget(proxyAddress: string, jsonRpcRequest: EIP1193ProviderRequestFunc, blockTag?: BlockTag): Promise<DetectProxyTarget | null>;
export default detectProxyTarget;
